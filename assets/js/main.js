// Erstelle eine Variable mit dem Namen carName, und weise ihr den Wert BMW zu.
// Gib die Variable mit der Konsole aus.

let carName;
carName = "BMW";
console.log(carName);

// Erstelle eine Variable mit dem Namen x, und weise ihr den Wert 150 zu. 
// Gib die Variable mit der Konsole aus.

let x;
x = 150;
console.log(x);

// Erstelle eine Variable mit dem Namen y, und weise ihr den Wert 50 zu. 
// Gib die Variable mit der Konsole aus.

let y;
y = 150;
console.log(y);

// Erstelle eine Variable mit dem Namen z, und weise ihr den Wert x+y zu. 
// Gib die Variable mit der Konsole aus.

let z;
z = (x + y);
console.log(z);

// Deklariere in einer einzelnen Zeile drei Variablen mit den folgenden Namen und Werten:
// let firstName;
// let lastName;
// let age;

// firstName = "John"
// lastName = "Doe"
// age = 35

let firstName = "John", lastName = "Doe", age = 35; 

// Gib die Variablen mit der Konsole aus.

console.log(firstName, lastName, age);
